	#include <stdio.h>
	#include <stdlib.h>
	#include <math.h>
	
	int main () {
		float N1, N2, soma;
		printf("Insira o primeiro numero: ");
		scanf("%f", &N1);
		printf("Insira o segundo numero: ");
		scanf("%f", &N2);
		soma= N1+N2;
			if (soma>=10)
			printf("Resultado: %0.1f\n", soma);
		system("pause");
		return 0;
				
		
		
		
	}
